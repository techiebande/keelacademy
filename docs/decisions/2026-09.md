# Decisions archive — 2026-09

Moved verbatim from build-state.md on 2026-09-07 (improvement plan M3.1).
Original log order preserved. New decisions are NOT authored here; they go to
build-state.md, which keeps the most recent entries and appends its oldest
here verbatim whenever its compact budget is exceeded.

- **2026-09-06 — All authored units deleted & authoring reset to zero:**
  - Removed all remaining authored units (0.2, 0.3, and 3.2.1) and their associated assets (`content/units/phase-0/0.2/`, `content/units/phase-0/0.3/`, `content/units/phase-3/3.2.1/`, `content/checks/3.2.1.*.yaml`, `content/rubrics/0.2/`, `0.3/`, `3.2.1/`, `content/golden/0.2/`, `0.3/`, `3.2.1/`, `content/routing/0.2.yaml`, `0.3.yaml`, `3.2.1.yaml`, `content/evals/guard/3.2.1.yaml`, and per-unit judge/retrieval/concierge prompts).
  - Retained the generic prompts (`concierge-guard.md`, `concierge-teach.md`, `retrieval-grade.md`) and all 56 curriculum map module definitions in `phases.yaml`.
  - Updated content validation tools (`validate.py`, `validate-rubrics.py`, `validate-routing.py`, `validate-guard-evals.py`, `dryrun-unit-checks.py`) to recognize and validate the clean zero-authored-units state without failing.
  - Updated smoke tests and app scripts (`smoke-map-checks.py`, `smoke-c1a-checks.py`, `dev-up.sh`, `platform/app/AGENTS.md`, `checkout/page.tsx`, `submit/page.tsx`) to reflect that zero units are authored on disk.
  - All 8 content validation gates, linters, and Next.js TypeScript compilation verified clean (green exit 0).

- **2026-09-06 — Unit 0.1 completely deleted & authoring reset:**
  - Removed all authored content, rubrics, golden calibration sets, prompts, routing rules, and FAQ assets for Unit 0.1 (`content/units/phase-0/0.1/`, `content/rubrics/0.1/`, `content/golden/0.1/`, `content/prompts/*0.1*`, `content/routing/0.1.yaml`, `content/faq/0.1.md`).
  - Retained Unit 0.1 in the curriculum map (`content/curriculum/phases.yaml`), diagnostic baseline, and database schema where curriculum architectural continuity requires it (renders honestly as planned/content-arriving in the dashboard).
  - Updated dependent test harnesses and assertions: `platform/grading/scripts/smoke-map-checks.py` authored units pin updated to `{"0.2", "0.3", "3.2.1"}`; `platform/grading/scripts/smoke-c1a.sh` and `smoke-c1a-checks.py` shifted conceptual unit test suites to Unit 0.2.
  - Updated app and dev scripts: `platform/dev-up.sh` seed enrollment removed 0.1; `platform/app/AGENTS.md` authored unit count updated to 3; `platform/app/app/checkout/page.tsx` and `platform/app/app/submit/page.tsx` default unit fallbacks updated to 0.2.
  - All 8 content validation gates, linters, and Next.js TypeScript compilation verified clean (green exit 0).

- **2026-09-03 — Unit 0.1 authored & verified via Backward Design subagent team:**
  - **UbD Architect:** Scoped target competency (3-sentence zero-jargon problem statement, stakeholder trilemma, HITL boundaries, 4 retrieval seeds).
  - **Assessment Engineer:** Built reference model brief for parallel entity Apex Freight Logistics (`worked-example/README.md`) and scaffolded student template (`completion/README.md`).
  - **Rubric Evaluator:** Formulated `content/rubrics/0.1/v1.yaml` (4 criteria), judge prompt `content/prompts/judge-0.1.md`, and 6 pre-graded golden calibration submissions (`content/golden/0.1/s01-s06`).
  - **Pedagogical Author:** Authored `learn.md` (Comeau arc, arithmetic of breakdown at 4k/month, reveals, 3-tier hint ladder), `unit.yaml`, and `faq/0.1.md`.
  - **Blind Playtester:** Conducted isolated cold playthrough without solution access; caught the "Purchasing Agent" regex landmine and expanded word budget (600–1,200 words), which were immediately resolved.
  - **Validation Battery:** `validate.py` (PASS), `validate-rubrics.py` (PASS), `validate-map.py` (PASS), `lint-lesson.py` (0 advisories), Next.js `typecheck` (0 errors).

- **2026-09-03 — Unit creation contract revoked & authoring reset:**
  - Deleted `docs/unit-creation-contract.md` and removed references across repo documentation.
  - Lesson authoring process and methodology reset to open/undecided.
  - An autonomous Backward Design (UbD) subagent team will preside over end-to-end curriculum planning, assessment engineering, content writing, and review.

- **2026-09-02 — Lesson UX Revamp Track (U1–U10) COMPLETE:**
  - **U1 (End-of-unit exit card):** `UnitExitCard` mounted after the last script phase; dynamic states for signed-out, passed gate with next unit, not passed yet with on-unit action, and phase-final.
  - **U2 (Phase-boundary exit markers):** `UnitBoundary` renders data-assembled mono anchor at each script phase boundary with read time and pause permission.
  - **U3 (Resume persistence):** `reading-position.ts` + `reading-tracker.tsx` save scroll position to `localStorage`; `resume-banner.tsx` alerts returning students; `reading-continue-card.tsx` on `/me` dashboard.
  - **U4 (Checkpoint hint ladders):** Sequential `<details className="reveal reveal-hint">` disclosures below the scratch box with focus management; cognitive hints authored in `3.2.1/learn.md`.
  - **U5 (Mastery states + cross-unit interleaved review queue):** `derive_seed_mastery` maps attempts to `unstarted` | `attempted` | `familiar` | `proficient` | `mastered` with decay on missed checks; `/practice/review/queue` returns interleaved seeds round-robined across all enrolled units.
  - **U6 (Review-queue surface in app):** Prior units' review seeds mix into the retrieval drill unlabeled; due reviews and mastery chips surface on the `/me` dashboard.
  - **U7 (Explain-it-back step):** `ExplainItBackCard` mounts after passed completion problem; `/practice/explain/evaluate` endpoint evaluates 2-3 sentence invariant explanations against the rubric.
  - **U8 (Code-figure polish):** `has-line-numbers` class and CSS counter gutters added to code figures only where a line range is authored.
  - **U9 (Lesson coda + advisory lesson lint):** `::: coda <title>` marker in content parser renders distinct design note card; executable `content/tools/lint-lesson.py` gives advisory feedback on apparatus pacing, coda presence, and heading cadence.
  - **U10 (Typography numbers pass):** Lesson body 17px, line-height 1.6 (mobile 1.45), code max(0.85em, 13px), h2 top margin ~3 body lines of air; WCAG 1.4.12 override resilient; contrast battery green.
  - All verified via `npm run lint` (exit 0), `npm run typecheck` (exit 0), `npm run build` (exit 0), `validate.py` (all pass).

- **2026-09-01 — Remediation pass: punctuation and voice sweep of `content/`, one student-facing path leak fixed, a11y coverage widened, full battery re-proved green.** THE SWEEP: every em and en dash in `content/` is now either removed or a documented deliberate keep. Rewritten this round: all three persona backstories (`discovery-call.md`, `technical-stakeholder.md`, `business-owner.md`; their `Trigger.` headings now read `2. **Trigger. Rejection of Hand-Wavy / Vibe Claims:**` and so on, because `simulation/engine.py:649` injects the backstory file whole into the persona system prompt, so punctuation there propagates into student-visible dialogue); unit 3.2.1's `learn.md` (447 lines, six comment lines) and its third retrieval seed, kept byte-identical in `smoke-routing-checks.py` and `smoke-recheck-checks.py` (`SEEDS CONSISTENT`); 24 comment and docstring lines across the ten Python fixtures under `3.2.1/{completion,worked-example}/`; five `content/tools/validate*.py` runtime output strings; three golden READMEs; and eleven `grade.yaml` `grader_notes`. FROZEN DATA PROVED INTACT BY HASH, not by eye: the fixture corpora (`INVOICE_NOTES` `4f343c47`, `_FAKE_OUTPUTS` `521efb69`, `SYSTEM_PROMPT` `01e32d8a`, identical in both fixture dirs) and all twenty golden and adversarial `grade.yaml` files hashed over every field except `grader_notes` (`ALL FROZEN FIELDS INTACT`, 0 problems; the hash recipe needs `json.dumps(..., default=str)` because `graded_on` is a date). Fixture behaviour unchanged: worked-example `5 passed`, completion `5 failed, 2 passed` with the same five test names, both pipelines conserving 10 notes in and 10 objects out. THE NINE DOCUMENTED KEEPS, which are not defects and must not be "fixed" by a later session: seven simulated student submissions (`golden/3.2.1/{s03-guided-decoding,s09-hardcoded,s15-manual-validation}/extract_claims.py`, `adversarial/3.2.1/{a1-prompt-injection,a2-rubric-parrot,a5-dead-code-decoy}/extract_claims.py`, `adversarial/3.2.1/a4-test-gaming/tests/test_build.py`), which are calibration inputs and must keep reading like real student work, and the two frozen `notes.py` corpora, whose dashes are the messiness the extractor exists to survive. Two in-app keeps for the same reason: `lib/content.ts:303`'s H1 regex tolerates a legacy em dash as a parsing fallback (all four authored H1s now use colons), and `app/page.tsx:61` renders `rubric-0.1.yaml` as an `aria-hidden` code-window title, where naming the real versioned artifact is more credible to an engineer audience than prose would be. THE STUDENT-FACING FIX: `components/unit/unstuck-section.tsx` rendered `entry.fix_ref` as a visible chip, so a student stuck on unit 0.2 was shown the repo path `faq/0.2.md#progress-vs-verification`, and the `ContentArriving` fallback interpolated the same path into its sentence. Both are gone. The chip is now a sentence-case `Fix` label plus an `aria-hidden` chevron using `group-open:rotate-180` (the `group` class was already on the `<details>`, and `list-none` on the `<summary>` had removed the native disclosure triangle, so the leaked path had become the only expand affordance); the fallback reads "The fix for this symptom is planned but not written yet." `fix_ref` still drives `anchorOf()` and the React key, so FAQ resolution is untouched. Verified in the rendered HTML: zero occurrences of the path in any element or attribute, the chip and the chevron both present. The two remaining occurrences in the page source are the React key inside the RSC flight payload, which is true of any key value and is not student-visible. A11Y COVERAGE: `scripts/a11y-static.mjs` `DEFAULT_ROUTES` widened from 15 to 18 with `/submit`, `/gallery/1`, and `/checkout`, giving **216 passed, 0 failed** (12 checks per route). `/me` and `/submissions/1` were deliberately not added: unauthenticated they render the identical sign-in page that `/checkout` already covers, and their authenticated views are outside what a fetch-only battery can reach, which is a standing limitation of this gate. THE FULL BATTERY, every number re-run by the reviewing session rather than accepted from a report: six content validators exit 0 (`validate.py` "All results as expected.", rubrics 4, gates 2, routing 4, diagnostics, guard evals 1); `check-concierge-structural.py` "ALL CONCIERGE STRUCTURAL CHECKS PASS"; `tsc --noEmit` clean; `eslint` no findings; demo-practice **68/0**, demo-map **33/0**, demo-gates **29/0**, demo-rebate **26/0**. Hygiene at close: zero containers, zero `/tmp/keel-*-demo` dirs, zero `next dev` processes.

- **2026-09-01 — Two harness defects found while re-proving the demo battery (both cost a false failure before being understood; log them so the next session does not re-diagnose them).** (1) **`demo-practice.sh prove` is not idempotent.** Run twice against the same live stack it reports 11 failures, all of them assertions whose expected value is a first-run count or state (`retrieval attempt 2 recorded in database (2 total retrieval attempts)`, the rendered attempt-history greps, and the `SCAFFOLD ROUTE ACTIVE` route assertions, which depend on the scaffolded student not having already completed the route). A fresh `teardown` then `setup` then `prove` gives 68/0. The failures are test-isolation artifacts, not product regressions, and a session that sees them must re-prove on a clean stack before concluding anything. (2) **Only one demo stack can hold the learner app at a time, and `demo-map.sh` does not say so.** Next.js 16 refuses a second `next dev` in the same project directory ("Another next dev server is already running"), so the four demos must be run one at a time with a teardown between. `demo-gates.sh:140` and `demo-rebate.sh:140` pre-flight this with an explicit `pgrep` check and a named failure; `demo-map.sh` has no such guard, so it fails 90 seconds later with the misleading `FAIL: learner app never became ready`, and the real cause is only visible in `/tmp/keel-map-demo/logs/app.log`. Adding the same guard to `demo-map.sh` and `demo-practice.sh` is an open cleanup item. Related and already load-bearing in this session's method: piping a `set -euo pipefail` demo script into `head` or `tail` lets SIGPIPE kill the script and report a false failure, so capture output to a file and grep the file instead of piping it.

- **2026-09-01 — Open items that are the founder's to decide, surfaced rather than actioned.** (1) `content/commitment/commitment.yaml` is orphaned: `components/commitment-form.tsx` hardcodes three checkout acknowledgments and the YAML declares four that differ in substance, so wiring the content file in would change what a student legally agrees to at payment. (2) Terms and Privacy pages need a legal entity name, a jurisdiction, and data-processor facts that exist nowhere in the repo. (3) No contact address exists anywhere in the codebase (`mailto:`, `support@`, `hello@` are all absent), which is why no copy promises "email us". (4) `scripts/a11y.mjs` (Playwright + axe-core) cannot run in this environment for want of `libnspr4.so` with no passwordless sudo, so `a11y-static.mjs` is the accessibility gate that actually runs; the axe battery remains unproven here. (5) **Guard-eval coverage is unit 3.2.1 only.** `content/evals/guard/` contains exactly one file, `3.2.1.yaml`, so the three Phase 0 guard prompts (`concierge-guard-0.1.md`, `-0.2.md`, `-0.3.md`) pass the structural clause greps but have never faced the live adversarial battery that S3.6 established as the standard. Authoring `content/evals/guard/0.1.yaml` and its siblings, with detectors keyed to the conceptual deliverables rather than to `extract_claims.py` identifiers, is the natural companion to C1b. (6) Unit 3.2.1 still names its deliverable `extract_claims.py` and its model `ClaimExtraction` while the anchor domain is invoice reconciliation. This is reported, not silently migrated: renaming reaches 15 golden fixtures, 5 adversarial fixtures, the checks file, the rubric, the judge prompt, and the guard eval, and it would invalidate the frozen hashes above, so it needs to be its own milestone with its own re-calibration.

- **2026-09-01 — LESSON DELIVERY REBUILT (owner instruction, from a student's point of view: "i don't like the way the lessons are delivered"). Approved shape: one continuous typeset lesson.** THE DIAGNOSIS, all confirmed in code before anything changed: `.lesson-prose` set no `max-width`, so lesson text filled the 1280px shell at roughly 145 characters per line; the authoring freshness taxonomy was student-facing (a "THE THREE LAYERS" eyebrow, `Concept core` / `Applied context` / `Tool specifics` section names, a per-section `CHECKED <date>` chip, and an intro paragraph in every lesson explaining the layer system before teaching anything); `learn-section.tsx` opened only the first of three `<details>` accordions, hiding two thirds of the teaching behind clicks with no indication of length; every answer was pre-revealed, so "Predict, then check" was advice the page made impossible to follow; gotchas and checkpoints rendered as blockquotes in `--color-sage-40`, dimmer than the `--color-sage-60` prose around them; and `parseLesson` split sections on `---` rules with a literal `"Section"` fallback, so unit 0.1's stray `---` produced **four** sections immediately after the intro promised three. WHAT LANDED. (1) `lib/content.ts`: `Lesson.layers` is replaced by `Lesson.sections`, each with an id, a name taken from its own `## ` heading (stripped from the body so it stops printing twice), the `###` headings for the contents rail, and a list of typed `LessonBlock`s. `parseLessonBlocks` recognises the beats the authored lessons already use and nothing else: `> **Gotcha: <title>**`, a `> **Predict, then check.**` blockquote plus the paragraph after it as the answer, and a bold-led paragraph followed by `One good answer:`. Unrecognised markdown stays prose, so plain authoring still renders plainly. Rail ids come from the *rendered* HTML rather than the markdown, so a `###` inside a fenced block can never produce a dead link, and they are deduped lesson-wide because the a11y gate fails a repeated id. A bare `### Checkpoint` heading directly above a checkpoint is dropped at render time, because the beat carries its own label and the heading would otherwise take a slot in the rail. `last_verified` is read by index against `["concept_core","applied_context","tool_specifics"]` rather than by slugifying a display name, so renaming a heading cannot silently drop the dates; `oldestVerified()` exposes the oldest of the three as the lesson's single freshness line. Tables are wrapped in `.table-scroll` so they survive the narrow measure with their semantics intact. (2) `components/unit/learn-section.tsx` renders one continuous lesson: a sticky contents rail beside the prose at `lg` and up, stacked above it below that, all three sections open and in order, each an `h3` with its `###`s as `h4`s. The accordions, the layer eyebrow and the per-section chips are gone, replaced by one "Checked for accuracy <oldest date>" line. (3) New `components/unit/lesson-contents.tsx` (client, modelled on the existing `section-nav.tsx`: same rAF-throttled scroll listener, same read-line band) and `components/unit/lesson-callout.tsx` (server, **zero client JS**: native `<details>` and a labelled `<textarea>`, so a checkpoint works through a failed hydration). (4) `app/globals.css`: the measure is `--lesson-measure: 35em`, deliberately **not** `ch` and deliberately **not** on `.lesson-prose` (the worked example reuses that class for a comparison table that needs the shell width). `ch` is the width of a zero, which in Inter runs wider than its average lowercase letter, so the conventional "72ch" column actually reaches the high eighties. `.lesson-prose blockquote` moved off `--color-sage-40` onto `--color-moss-70`: a quoted beat is never less legible than the prose around it. (5) Grading detail moved out of the reading flow without losing a fact: the four-modes grid in `verify-section.tsx` collapses behind a reveal under one sentence naming what applies to this unit, and each rubric criterion keeps its visible name with its description and "Must quote" line behind a reveal. The `Graded on` spec now reads `RUBRIC REVIEW, 3 CRITERIA` for a conceptual unit instead of the honest-but-useless `0 CHECKS, 3 CRITERIA`, and the unit header's third printing of phase and duration is gone. CONTENT EDITS HELD DELIBERATELY MINIMAL, and this is the load-bearing constraint of the whole change: `platform/grading/practice/server.py` feeds `learn.md` to the retrieval-drill judge, split by `split_lesson_sections` on any `#`-`######` heading and scored per seed keyword by `select_lesson_excerpt` with heading hits weighted 5x. So the reveals are a **render-side transform**, not a content rewrite. All four `learn.md` files keep their prose; only the intro meta-paragraph and its numbered list were deleted, the three `##` names were rewritten to student-facing ones (0.1: `The idea` / `OmniSupply's situation` / `What you will produce`; 0.2: `Why grading works this way` / `What that means for your submissions` / `Tracking your own progress`; 0.3: `Why the environment ships with the code` / `The container you build against` / `Setting it up, step by step`; 3.2.1: `Why prose is not an interface` / `The same idea in the OmniSupply pipeline` / `The APIs and patterns you will type`), and 0.1's stray `---` was removed. `unit.yaml`, `content/schemas/unit.schema.json` and the `last_verified` keys are untouched: the three-layer model remains the authoring and freshness contract, it is simply no longer shown to students. Inline `code` in a lesson now sets `overflow-wrap: anywhere` (and `pre code` resets it, because a code block scrolls instead): the longest unbreakable token in the four lessons is `TypeAdapter(list[ClaimExtraction])` at 34 characters, which is wider than a 320px viewport once a callout's padding is taken off, and the 320px reflow rule admits no horizontal scroll. NOT IN THIS CHANGE: persisted per-section reading progress. There is no table for lesson reading state, only practice attempts and verdicts, so the rail reports scroll position and says so in its `aria-label` rather than implying learning.

- **2026-09-01 — UNIT SCRIPTS: content owns every word a student reads (owner instruction, "i want lessons to feel like a teacher in front of students", reference voice named as Josh Comeau's blog).** THE DIAGNOSIS, and it is structural rather than a matter of wording. A unit page was six components in a fixed order, each opening with `STEP 0n` chrome and a hardcoded lead sentence: `learn-section.tsx:45` "Read this once now, and once more before you start the deliverable.", `practice-section.tsx:52` "Warm up before building...", `build-section.tsx:18` "What you ship for this unit...", `verify-section.tsx:60` "Nothing here is hidden from you...", `unstuck-section.tsx:26` "The failure modes this unit produces...", `concierge-panel.tsx:142` "An assistant that has read this unit and nothing else." One string served every unit, so it structurally could not refer to the concept the student had just learned, and each lead therefore described its own apparatus instead. That is the whole reason the page read like a machine handing off between departments rather than a teacher saying "right, you have seen why this breaks, now watch me fix it on something smaller." A data-derived bridge template was proposed and rejected by the owner outright; the fix taken instead moves the boundary, to **content owns every word a student reads, and the app owns structure, data and state.** THE FORMAT. `learn.md` may now be a **unit script**: one continuous lesson covering all six phases, where a line starting `::: ` is a marker and everything else is markdown that keeps every convention the renderer already understood. Three marker kinds: `::: phase learn|practice|build|verify|unstuck|ask` opens a landmark section and closes the previous one, emitting the same `<section id data-keel-section class="scroll-mt-28">` the fixed layout emitted (`ask` maps to the `concierge` id, because `SECTION_ANCHORS`, every `#learn` style anchor and the demo greps key off those ids); `::: <slot>` injects apparatus, from a closed list of twelve (`route`, `worked-example`, `workbench`, `retrieval`, `deliverable`, `submission`, `prove-it`, `grading-modes`, `checks`, `rubric`, `unstuck`, `ask`); and `::: aside <title>` up to a closing `:::` is a collapsible digression. It stays in `learn.md`, with no new file and no new `unit.yaml` key, because `unit.schema.json` sets `additionalProperties: false` and because `unit.yaml`'s `learn:` key plus `get_unit_learn_text` already resolve that one path. Markdown is not schema-validated, so no validator changed. THE THREE OWNER DECISIONS, taken before implementation and binding on every future script. (1) **Register: keep Keel's copy bans, take Comeau's pedagogy.** The 2026-08-31 copy decision and `checkCopyRules` are unchanged, so still no em or en dashes, no exclamation marks, no hype verbs, no internal architecture vocabulary; what is newly explicit and encouraged in lesson prose is first person ("we" for a walkthrough, "I" for an opinion), contractions, direct address, and questions as connective tissue. (2) **Interactivity: static beats, one real run.** Predict-then-check, scratch boxes and asides stay server-rendered with zero client JS so they work signed out; the Docker-backed workbench is placed once, at the point in the script where the student is meant to actually run code. No new execution path. (3) **Lesson text: re-sequence and rewrite freely.** The existing lesson is the source of facts, not of sentences. WHAT LANDED IN CODE. `lib/content.ts` gains `parseUnitScript`, which returns `null` unless the file contains a `::: phase` line: that is how 0.1, 0.2 and 0.3 keep `parseLesson` and the six fixed components with **no per-unit flag anywhere**, and it is why two renderers now run side by side. It reuses the h1 regex, `renderMarkdown`, the fence-aware `splitParagraphs`, `unquote`, all four marker regexes, `parseLessonBlocks`, `slugify` and `shiftHeadings` rather than reimplementing any of them. Two deliberate differences from `parseLesson`: script headings are **not** shifted, because a script authors the page's own section headings, so `##` renders as `h2` under the page `h1` and `###` as `h3` while the apparatus cards bring their own `h3` and `h4`; and generated ids are deduped against a new `SCRIPT_RESERVED_IDS` list (`main`, the six phase anchors, the `<phase>-body` wrappers and the apparatus cards' own anchors), because `slugify("Practice")` would otherwise collide with `id="practice"` and fail the duplicate-id gate. An unknown slot name warns at parse time and renders nothing, never text: an invented apparatus name must not become visible content. Each of the five section components was split so its apparatus is exported piece by piece with markup, props and copy untouched (`PracticeRouteStrip`, `WorkedExampleCard`, `CompletionWorkbenchCard`, `RetrievalDrillCard`, `DeliverableCallout`, `SubmissionContractCard`, `ProveItCard`, `GradingModesCard`, `AutomatedChecksCard`, `RubricCard`, `UnstuckList`), which is what preserved all 20 `/units/3.2.1` demo needles; the components themselves stay, composing their own pieces, and still serve the three unconverted units. `concierge-panel.tsx` takes an `embedded` prop that suppresses both the `STEP 06` header block and the duplicate `<section id="concierge">` wrapper, while keeping the mode chip in both modes because `demo-practice.sh:720` greps `WORKS IT THROUGH WITH YOU`. Two new server components: `unit-script.tsx` walks the phases, attaches the contents rail to the first phase with two or more `##` entries (a second rail repeating the six words `SectionNav` already shows would be noise), and emits `Checked for accuracy <date>` at the end of the first phase automatically **so an author cannot forget the honesty signal**; `lesson-aside.tsx` renders one aside as `details.callout.callout-aside.reveal.reveal-flush`. `page.tsx` builds a `slots: Record<string, ReactNode>` from data it already fetched, which keeps every prop where it is and keeps the parser free of React, then branches `script ? <UnitScript/> : <the six sections>`; the header, `SECTION_ANCHORS` and `SectionNav` are untouched. `globals.css` adds only `.unit-script > * + *` for rhythm (deliberately no `max-width`: the prose carries its own 35em measure and the apparatus needs the shell width) with a 140px `scroll-margin-top` on script `h2`/`h3`, plus `.callout-aside`, which **must be declared after `.reveal-body`**: `.callout` sets `padding: 20px 24px`, `.reveal` later sets `padding-top: 14px` and `.reveal-flush` later still sets it to 0, all single-class, so source order is the only thing restoring the aside's top padding. `platform/grading/practice/server.py` gains `strip_script_markers`, applied inside `get_unit_learn_text`, which is the single entry point for retrieval grading (`:1112`), recheck (`:1396`) and the concierge (`:1551`): marker lines are page structure, not teaching, and the helper is fence-aware so a lesson can quote a marker while explaining the format. Verified in this session that the judge's text contains zero `:::`. THE SCRIPT ITSELF, unit 3.2.1 only, deliberately. `content/units/phase-3/3.2.1/learn.md` is rewritten from 447 lines to 421 as a script, in the beat order the format exists for: the failure shown before it is named (a working Phase 2 extractor whose output nothing downstream can read), a predict-then-check on the naive `Respond ONLY with JSON` plus `json.loads` fix, the cause named with the takeaway bolded, three asides for the shortcuts a reader will actually reach for ("Can I not just strip the fences and retry?", "Why validate what the provider already constrained?", "Where does the retry go, then?"), then authored bridges into every piece of apparatus. Every technical claim, code sample and number from the previous version survives: the five prompt-promised-JSON failure modes, the run-it-100-times experiment, the two enforcement points and why you want both, the four responses to validation failure with the CLM-88231 auditor illustration, the `inputs -> [ generate -> validate -> success? pass through : fallback + log ] -> outputs` shape, conservation and accountability as testable properties, the `ClaimExtraction` model with `extraction_failed`/`failure_reason` as schema-encoded fallback, the ASCII pipeline diagram, the full CLM-20841 walkthrough down to the `2026-08-21T14:03:11 FALLBACK` log line, the OmniSupply money-and-rebate-deadline stakes, the 2.4.2 and 1.5.1 reuse, the `FRESHNESS-AUDITED` comment verbatim, JSON mode vs schema-constrained families with all three provider samples, the strict-mode rules, enum-constrained categoricals, vLLM and Ollama `guided_json`, `model_json_schema()` as single source of truth, the six Pydantic v2 calls, the `extract()` fallback function and `test_twenty_in_twenty_out_no_silent_drops`. Three `##` sections are kept in the learn phase, so `last_verified`'s `concept_core`/`applied_context`/`tool_specifics` keys and the three-entry contents rail both still hold; `unit.yaml` is untouched, including all five `retrieval_seeds`, which are the judge's contract. THE GRADING CHECK THAT MATTERED MOST, since the file the judge reads was rewritten: `select_lesson_excerpt` was called directly for each of the five seeds against the new text, and every one still selects teaching-phase sections including its own target section (seed 2 now resolves to a single section, `JSON mode vs structured outputs: what each actually guarantees`, which is tighter than before), with no build, verify or practice bridge ever winning. The reason headings did not have to be contorted is worth recording: `select_lesson_excerpt` ranks by distinct-keywords-then-total where a hit counts in heading **or** body, so coverage comes from the teaching prose using the seed vocabulary, not from keyword-stuffed headings. `SEEDS CONSISTENT` re-proved by parsing `SEEDS` out of both smoke scripts and comparing to `unit.yaml`. ONE DEMO GREP UPDATED, which the UI/UX decision expects rather than forbids: `demo-practice.sh:718` matched the heading string `"Ask about this unit"`, and a script authors its own heading, so the needle is now `concierge-question`, the question box's id, which only renders for a signed-in enrolled student and therefore proves more than the string match did. The check name is unchanged. VERIFIED THIS SESSION: `tsc --noEmit` clean, `eslint` no findings, `a11y-static.mjs` **216 passed, 0 failed** with `/units/3.2.1` alone at 12/12 (36 headings descending without a skip, 40 unique ids, no em or en dashes, no exclamation marks), six content validators exit 0, `check-concierge-structural.py` all pass, and the rendered page confirmed to emit the six phase sections in order with three asides, three teaching beats each carrying a scratch box above its reveal, and every apparatus slot resolving. STILL OWED: the four-demo battery, which is blocked on a `next dev` the owner is using on :3000 (`demo-practice.sh:181` pkills it during setup and Next 16 refuses a second instance in the same project directory). NOT IN THIS CHANGE, on purpose: 0.1, 0.2 and 0.3 stay on the current renderer, so `learn-section.tsx`, `SectionHeading`'s `stepNumber` and all six hardcoded leads survive until the last unit is a script and get deleted then. One unit was converted first so that if the result did not obviously sound like a person, we learned it for the price of one unit rather than four.

- **2026-09-01 — UNIT PAGE LAYOUT: one text column, one rail, and section breaks that look like section breaks (owner instruction: "what about the structure and the styling of the unit page itself. it currently doesnt feel 'flowing nturally'").** The unit-script entry above fixed the words. This entry fixes the container, which had not changed and still read as a dashboard. THE DIAGNOSIS, all measured in code before anything moved. (1) A **264px unannounced horizontal jump** at the learn to practice boundary: `unit-script.tsx` picked `railPhase = phases.findIndex(p => p.contents.length >= 2)`, so only the learn phase sat inside `.lesson-layout` (a `216px` rail plus a `48px` gap) while every later phase was a bare `div.unit-script` at the full 1232px shell. The reading column's left edge moved partway down the lesson, which was the single most legible defect on the page. (2) The six-department taxonomy was still the loudest thing above the title: `SECTION_ANCHORS` fed `SectionNav` in a `sticky top-16` bar reading LEARN PRACTICE BUILD VERIFY UNSTUCK ASK, so the body had stopped naming the machinery and the chrome had not. (3) Width whiplash: roughly 578px of prose at `--lesson-measure: 35em` against 1232px apparatus cards, from different left origins. (4) Rhythm that carried no meaning: 32px inside a phase from `.unit-script > * + *` against 64px between them from `space-y-16`, so a change of activity looked like a slightly larger paragraph gap. (5) Six chrome zones before the first sentence: breadcrumb, a `UNIT 3.2.1` chip duplicating the breadcrumb's own last crumb, the `h1`, a mono metadata line, a bordered three-cell spec panel, then the sticky nav. WHAT MADE THIS SAFE TO CHANGE, established by grep first: nothing outside `page.tsx` consumes any of this chrome. No demo script, no a11y check and no grading path references `data-keel-section`, the `#learn` style anchors, `Unit sections`, `How to submit`, `Prerequisites`, `Unlocks`, `Graded on`, `lesson-contents` or `On this page`, and `SectionNav`/`SECTION_ANCHORS` are used by the unit page and nothing else. Only the phase ids matter, as deep-link targets, and they are unchanged. TWO OWNER DECISIONS, asked before implementing because both are subtractions a reader would notice. (1) **Drop the sticky section bar on script pages**, rail only. (2) **The rail lists top-level headings only**, eight of them, with no nested subheadings.

  WHAT LANDED. (1) `unit-script.tsx`: the `railPhase` lookup is deleted and the component emits **one** `.lesson-layout.unit-script-layout` grid holding the rail plus a single `#unit-script-body`, with all six phase `<section>`s inside that body. Their markup is otherwise unchanged, still `<section id data-keel-section class="scroll-mt-28">` with `unit-script unit-script-phase` added, so prose and apparatus now share one left edge from the title to the last line and the apparatus track is 968px rather than 1232px, which closed most of problem 3 for free. The rail's entries are `phases.flatMap(p => p.contents)` mapped to `headings: []`, so it lists the script's own `##` headings across every phase in document order: eight authored beats that fit without the rail scrolling inside itself, with the subheading anchors still in the document for anyone who links one. `targetId` is now the page-level `unit-script-body`, so the bar reports travel through the whole lesson rather than through the learn phase, and it is still labelled a scroll position. **No parser change was needed**, because `ScriptPhase.contents` already carried exactly this shape. (2) `page.tsx`: the `sticky top-16` nav renders only when `script` is null, so it still serves 0.1, 0.2 and 0.3, which have no headings of their own to navigate by. `/submit` loses nothing by dropping the nav's link, because `build-section.tsx:130` renders "Read how submitting works" as a `btn btn-accent` inside the submission contract. The wrapper drops `space-y-16` for a script and keeps `shell pt-12`. The header loses the `UNIT {id}` chip and its now-single-child flex wrapper, and the spec `dl` loses `rounded-lg border border-circuit-border bg-carbon-veil p-4` so three mono label/value pairs read as part of the header's own register instead of as an instrument; its x-gap is `gap-x-4 sm:gap-x-8` because `8 CHECKS, 5 CRITERIA` has 272px of shell to wrap inside at 320px. (3) `globals.css`: **`--rail-top` carries the sticky offset** rather than two hardcoded numbers. `.unit-script-layout` sets it to 88px, which clears the 64px site header on a page with no second bar, and both `.lesson-contents` and `.unit-script h2, h3` read `var(--rail-top, 136px)`, so the fixed layout keeps today's 136px with no extra rule and `LessonContents` takes a matching `readLine` prop defaulting to 150. A phase boundary is now `.unit-script-phase + .unit-script-phase` at 56px plus a 1px `--color-phosphor-blue-black` hairline, against 32px inside a phase: the visual language's own device, no shadow, and the script's first sentence after each rule does the labelling. (4) `learn.md` gains one authored `##` per phase so the rail can reach all six: `Working one before you build one`, `Your extractor, on twenty real reports`, `How this gets graded`, `When it breaks`. The learn phase's three `##` are untouched, so the `concept_core`/`applied_context`/`tool_specifics` freshness contract and `unit.yaml` are both unchanged. **These headings were written clear of the `retrieval_seeds` vocabulary on purpose**, because a heading hit counts 5x in `select_lesson_excerpt` and a bridge heading borrowing seed words would beat the teaching section that actually answers the seed. (5) One real bug found and fixed while verifying: heading ids and rail labels are read back out of the *rendered* HTML rather than the markdown (so a `###` in a fenced block can never reach the rail), which meant marked's escaping was never undone, so `The same idea on OmniSupply's actual traffic` printed `OmniSupply&#39;s` in the rail. A new `headingText` helper in `lib/content.ts` strips tags and decodes marked's closed entity set, and it is used by **both** renderers, so the fixed layout's subheading rail was fixed at the same time. VERIFIED THIS SESSION: `tsc --noEmit` clean, `eslint` no findings, `a11y-static.mjs` **216 passed, 0 failed** with `/units/3.2.1` at 12/12 (40 headings descending without a skip, 44 unique ids), all seven content validators exit 0, all eight rail hrefs matching a rendered `h2` id exactly, all six phase sections still emitting their ids and `data-keel-section` values, the sticky nav absent from 3.2.1 and present on all three of 0.1, 0.2 and 0.3 with their nested sublists and `space-y-16` intact, the judge's text still free of `:::` at 24 sections, all five seeds still resolving to teaching-phase sections including their own target with **zero** of the four new bridge sections ever selected, and `SEEDS CONSISTENT` re-proved against both smoke scripts. NOT VERIFIED BY ME, and stated as such: the 320px reflow and the focus-ring tab-through are reasoned about rather than eyeballed, and the two `lg:grid-cols-4` grids (`practice-section.tsx:170`, `verify-section.tsx:93`) are computed at roughly 233px per cell on the narrower track, which holds their 10px chips and short labels comfortably but has not been seen in a browser. STILL OWED: the four-demo battery, still blocked on the `next dev` the owner is using on :3000. NOT IN THIS CHANGE, on purpose: the 17 `card-dark` boxes. Deciding which apparatus is a genuine instrument that earns a border is a per-component judgement across five files, and it should be made against the new single column rather than bundled into the change that created it.

- **2026-09-02 — Uniform single-column measure, Hero cleanup, and light-mode color fix (owner instructions).** (1) Uniform measure: all sections, apparatus (.flow-apparatus), code blocks (.code-fig), diagrams (.diagram), tables (.table-scroll), and callouts (.callout, .callout-checkpoint) are unified to `grid-column: text` at `width: 100%`, removing the previous multi-tier blowout (`body` 1115px and `wide` 871px) so all elements share equal left and right spacing. ResumeBanner and UnitExitCard wrapped into the same uniform measure. (2) Hero cleanup: removed the redundant "In this lesson" beat list from `ChapterOpener` (the TOC margin rail remains the single home for beat navigation). (3) Light mode color fix: replaced the unreadable fluorescent neon lemon green (`#7fee64`, 1.16:1 contrast) on light backgrounds with a deep, crisp forest green (`#166534`) that clears 6.9:1 WCAG AAA contrast across text, buttons, borders, and focus rings. Added button and chip text color/hover overrides for light mode.

- **2026-09-05 — Visual and copy polish pass complete across all 18 routes; full a11y static battery green (216/216 passed).** Swept all student-facing UI pages, components, and curriculum map data for copy rules and visual polish. Removed all em-dashes and en-dashes outside of code/pre/aria-hidden blocks across 20+ TSX components and pages, and cleaned curriculum phase data in `content/curriculum/phases.yaml` without mutating frozen unit fixtures or `curriculum.md`. Corrected `platform/app/README.md` from obsolete "UI/UX STATUS: UNDECIDED" to document the established dark engineering-console design system. Verification: `tsc --noEmit` clean, `eslint` clean, `python3 content/tools/validate-map.py` clean, `npm run test:a11y:html` **216 passed, 0 failed** across all 18 routes.

- **2026-09-06 — High-fidelity Mermaid diagrams upgrade across Unit 0.1.** Replaced rudimentary ASCII text blocks with crisp, accessible Mermaid flowcharts rendered via the Keel design system (`.diagram-frame` with dark phosphor/carbon palette tokens) and accessible captions (`<figcaption class="figure-caption">`): (1) Figure 1: The Multi-Channel Intake Bottleneck at Sarah's Desk (`learn.md`), (2) Figure 2: The Leadership Stakeholder Trilemma (`learn.md`), (3) Figure 3: Quantitative Human-in-the-Loop (HITL) Triage Routing (`learn.md`), and (4) Figure 1: Apex Freight Quantitative HITL Routing Pipeline (`worked-example/README.md`). Polished Figure 1 from wide `flowchart LR` with nested subgraphs (which caused horizontal SVG downscaling to ~8px blurry font) to a clean vertical `flowchart TD` architecture with bold primary labels and concise operational summaries that render at 100% full-scale 14px text. Enhanced `platform/app/scripts/check-mermaid.mjs` to execute from the workspace root or `platform/app` and added multi-instance DOMPurify stubbing for pnpm nested modules. Verification: `node platform/app/scripts/check-mermaid.mjs` (4/4 passed, 0 failed), `python3 content/tools/lint-lesson.py` (0 advisories, all pacing checks pass), all 8 content validation scripts exit 0, and `npm run test` (`tsc --noEmit` and `eslint`) passes cleanly.

- **2026-09-06 — Mermaid diagram text legibility and foreignObject styling resolution.** Resolved root causes of illegible/invisible diagram node text and labels: (1) Fixed `THEME_VARIABLES.fontFamily` in `platform/app/components/unit/mermaid-runtime.tsx` from `var(--font-inter-variable)` to standard CSS font stack `"Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif"` (HTML5 Canvas 2D `ctx.font` text measurement treats `var(...)` as an invalid font string, collapsing bounding box dimensions and causing SVG `<foreignObject>` text to be truncated or hidden). (2) Enforced high-contrast diagram rendering in `platform/app/app/globals.css`: pinned `.diagram-frame` background to `#0f1211 !important` and text color to `#d5dad2 !important` so light theme switches cannot invert dark diagram surfaces; added `overflow: visible !important` to `.diagram-svg foreignObject`; added explicit text color styling (`color: #d5dad2 !important; font-family: Inter...`) across `foreignObject` `div`, `span`, `p`, `.nodeLabel`, and `.label`, with `#ffffff !important` on `b` and `strong` tags and `#cbd5e1 !important` on edge labels. (3) Standardized Mermaid arrow syntax in `content/units/phase-0/0.1/learn.md` from non-standard `==>|"..."|` to `-->|"..."|`. Verification: `node platform/app/scripts/check-mermaid.mjs` (4/4 passed), `npm run test` (typecheck + eslint 0 errors), `npm run test:a11y:html` (216/216 passed across all 18 routes), and `lint-lesson.py` clean.

- **2026-09-06 — Figure 1 flow diagram removed from Unit 0.1 lesson (owner-directed).** Removed the multi-channel intake bottleneck flowchart from `content/units/phase-0/0.1/learn.md` to streamline the opening operational narrative. Renumbered subsequent figures cleanly (`Figure 1: The Leadership Stakeholder Trilemma`, `Figure 2: Quantitative Human-in-the-Loop (HITL) Triage Routing`). Added a crisp "Predict, then check" checkpoint after staging the naive automation failure to maintain Shiffman pedagogy pacing (<= 250 words per prose block). Verification: `node platform/app/scripts/check-mermaid.mjs` (3/3 passed), `python3 content/tools/lint-lesson.py` (0 advisories, all pacing checks pass), `npm run test` (typecheck + eslint clean), and `npm run test:a11y:html` (216/216 passed).

- **2026-09-06 — Removed Checkpoint 1 from Unit 0.1 opening dock section (owner-directed).** Removed the early "Predict, then check" checkpoint at line 29 ("When a customer return queue falls two to three days behind in e-commerce, what actually happens to company cash?"), allowing the 7:00 AM warehouse dock narrative and cash leakage analysis to flow continuously as direct prose into Section 2. Verification: `lint-lesson.py` clean (0 advisories, all pacing checks pass), `npm run test` (typecheck + eslint clean), and `npm run test:a11y:html` (216/216 passed).

- **2026-09-06 — Removed all checkpoints from Unit 0.1 lesson (owner-directed).** Completely removed all remaining `> **Predict, then check.**` checkpoints (Section 2 automated failure checkpoint and Section 3 stakeholder trilemma checkpoint) from `content/units/phase-0/0.1/learn.md`. The lesson now delivers a unified, unbroken narrative with direct prose and clean lists. Tightened Section 2 phrasing to keep word count under the 250-word pacing target without apparatus interruption. Verification: `lint-lesson.py` (0 advisories, all pacing checks pass), `node platform/app/scripts/check-mermaid.mjs` (3/3 passed), `npm run test` (typecheck + eslint clean), and `npm run test:a11y:html` (216/216 passed).

- **2026-09-06 — Lesson authoring subagent voice updated: Josh Comeau replaced with Shiffman style:**
  - Replaced pedagogical authoring voice across repository contracts (`AGENTS.md`, `platform/app/AGENTS.md`, `docs/lesson-flow-spec.md`) and subagents with Daniel Shiffman's teaching style, sourced from `.agents/skills/shiffman-style-lessons`.
  - Installed `.agents/skills/shiffman-style-lessons/` into repo workspace with core skills, voice guide, and worked examples.
  - Reconfigured the lesson authoring subagents (specifically `pedagogical_author` on the Backward Design team) to strictly follow Shiffman mechanics: live-build narration, visibly working through unexpected bugs, ask before tell (predict-then-reveal), alive/concrete domain entities, honest confusion, collaborative "we"/"let's" tone, and ending on an open challenge/invitation, while keeping Keel copy bans (no em/en dashes, no corporate buzzwords, no exclamation mark spam).

- **2026-09-06 — Cumulative Curriculum Continuity Architecture instituted:**
  - Established `content/curriculum/ledger.yaml` (Cumulative Curriculum Ledger) to solve the "amnesiac tutorial problem" without context bloat: tracks the learner's progressive project working tree (`omnisupply-system/`), unlocked concepts, forbidden assumptions (anti-prerequisites), active retrieval seeds, and parallel entity specifications (`Apex Freight Logistics` for worked examples).
  - Configured 3-tier memory model: (1) compact ledger (~100 tokens/unit), (2) full immediate predecessor Unit N-1 (~3,000 tokens) for narrative/voice continuity, (3) on-demand tool lookups (`view_file`/`grep_search`) for specific past snippets.
  - Reconfigured all 5 Backward Design subagents (`ubd_architect`, `assessment_engineer`, `rubric_evaluator`, `pedagogical_author`, `blind_playtester`) to enforce Step 0 prior-art audits, incremental codebase accumulation, canonical conventions (integer cents, standard prefixes), and strict "continuity violation" gating during cold playthroughs.

- **2026-09-06 — Unit 0.1 repair package: every defect from both reviews fixed and offline-verified. Full-stack enrolled playthrough still pending (Docker down).**
  - Runtime fixes: app `resolve()` now accepts unit-local refs, a leading `content/` prefix, and worked-example refs that name the file OR the directory (worked example + rubric card now render; the practice server's `get_unit_learn_text`, manifest, and scaffold-target readers got the same unit-relative resolution, which also fixed a latent grading crash on `learn.md`); `judge-0.1.md` rewritten to the criteria-ARRAY contract (`unit`/`overall`/`overall_rationale`/`criteria[]`) the parser and fake judge already spoke; `content/routing/0.1.yaml` authored off the recovered 3.2.1 template; the conceptual workbench got a prose-answer mode (brief.md editor, `answer` submit path, no `schemas.py` default) keyed off `manifest.kind`.
  - Assessment fixes: rubric v1 gained a 5th criterion `brief-format-compliance` (600-1,200 words, four mandated headings verbatim and in order, zero em/en dashes, zero exclamation marks); judge prompt gained matching boundaries; the practice server now prepends platform-computed Deterministic Submission Facts (word count + heading list) to the grading prompt so the judge never counts for itself; golden set recalibrated — s01 rewritten as a fully compliant textbook pass (~840 words, five intake formats), s02-s05 rebuilt format-compliant so each isolates exactly its target criterion, s06 renamed `s06-terse-format-fail` (~269 words, renamed headings, content criteria pass) as the format-fail sample; conceptual attempt result cards now show per-criterion evidence instead of the overall rationale repeated.
  - Continuity fixes: `curriculum.md` fully transitioned to OmniCart Operations (domain vocabulary mapped: vendor invoices/POs/supplier SLAs -> merchant payout invoices/customer orders/store return policies; the rendered 0.1 anchor line is dash-free), plus OmniSupply swept from phases.yaml capstone, ledger (parallel entity now Apex Freight Logistics, matching the worked example), gate capstone summary, commitment screen, personas, simulation engine + fake judge + both simulation smoke scripts (string contract renamed in lockstep), analytics/digest titles, app pages (home/faq/map/curriculum/simulations/simulation workbench), schemas/examples/validators, dev-up seed, and build-plan/school-architecture. build-state.md history entries intentionally left as written.
  - New plumbing for unit sequencing: `content/gates/unit-0-1.yaml` (unit_id 0.1, unlocks 0.2, no rebate; gate ids must be dotless slugs, hence unit-0-1) and the practice server now emits an idempotent `verdict.issued` event (same transaction as the attempt row, guarded by NOT EXISTS on prior pass) on conceptual passes so the gate engine can unlock 0.2 — code-unit practice attempts still emit only `practice.attempt_graded`.
  - Offline verification (all green): 8 content validators + lesson lint + Mermaid 3/3 + `npm run test` + `npm run build` + concierge structural checks; live battery with the fake judge and a SQL-logging psql stub — seeds return real question prompts, `/practice/route` returns a 4-step in_progress route, submitting golden s01 grades 5/5 through the criteria-array contract, and the SQL log proves attempt row + `practice.attempt_graded` + `verdict.issued` land in ONE transaction; rendered `/units/0.1` shows the worked example, the 5-criterion rubric card, drill questions (no slugs), the file-submission card (no repo/push/fixture cards), and the conceptual workbench with zero "not written yet" placeholders; copy-ban sweep over all touched learner-facing files clean.
  - Not verifiable this session (Docker down): the simulation smoke scripts' renamed assertions and the real enrolled playthrough (real Postgres enrollments, real proxy judge, gate-engine unlock of 0.2). Also noted: the retrieval drill's fake-judge canned feedback is generic smoke copy; the real judge grades against the lesson excerpt.

- **2026-09-06 — Unit 0.1 re-review (execution-verified): every recorded defect persists unrepaired; four new defects found. Verdict stands: NOT releasable.**
  - Live proofs this session (practice service started with stub DB; Next dev rendered against it; all review processes cleaned up): `/practice/route?unit=0.1` returns 404 `routing_rules_not_found` (content/routing/ still empty); `/practice/retrieval/seeds?unit=0.1` returns raw slug strings incl. stale `omnisupply-problem-statement`; `/practice/manifest?unit=0.1` returns `editable_files: []` for a conceptual unit while the workbench UI is a code-file editor defaulting to "Editing: schemas.py" whose submit posts `{}` -> 422 `answer_required`; feeding `extract_conceptual_verdict_json` a reply in judge-0.1.md's own mandated shape (criteria as OBJECT) is rejected ("missing or empty criteria array"), so every completion grading attempt will 502 MalformedJudgeError; rendered `/units/0.1` shows the worked-example and submission-contract "PLANNED / not written yet" placeholders (worked-example/README.md IS authored; the app appends README.md to the file path given in unit.yaml), the repo-naming/push-GitHub/fixture-corpus cards despite `submission: file`, and zero rubric card (unit.yaml's `content/rubrics/0.1/v1.yaml` does not resolve relative to content root; RubricCard renders null silently).
  - New defects beyond the recorded list: (1) rubric card silently absent (not just verify-section paths); (2) workbench hardcodes `schemas.py` as the active tab for conceptual units; (3) curriculum.md's unit anchor uses `### 0.1` (three hashes) while the app parser matches `#### 0.1 ` (four), so the anchor is silently null, and curriculum.md is still OmniSupply throughout; (4) s03 grade.yaml notes still say "unrecovered supplier overbilling" (OmniSupply-era language), and the judge prompt's five-format list ("emails") diverges from the completion template's five formats (merchant payout invoices).
  - Also re-confirmed: golden s06 passes at 263 words with non-mandated headings while the exercise mandates 600-1,200 words and exact headings (neither graded by rubric v1 or judge prompt); ledger parallel entity "Apex Express Delivery" != worked example "Apex Freight Logistics"; phases.yaml capstone still OmniSupply.
  - Note: the pre-existing dev server on port 3000 (pid 235435, env unknown, Docker down so its stack was inert) was stopped to run the controlled render; not restarted. Structural validators, lesson lint, Mermaid 3/3, `npm run test`, concierge structural checks all re-run green this session, consistent with the gap already recorded: they do not cover these semantic/end-to-end failures.

- **2026-09-06 — Unit 0.1 review rejected the current release candidate:**
  - Runtime blockers: `worked_example` and rubric paths do not resolve in the learner app; the conceptual workbench submits no answer; the Unit 0.1 judge prompt emits a criteria object while the practice parser requires a criteria array; and no routing rules exist for the authored route surface.
  - Learner-facing contract defects: retrieval drills display internal slug strings, including stale `omnisupply-problem-statement`; the build surface gives repo/check instructions despite `submission: file`; and rendered placeholders claim the completed worked example and submission contract are not authored.
  - Assessment blockers: the exercise requires 600-1,200 words and exact headings, but the rubric does not grade either constraint and the golden set marks a 263-word differently headed submission as passing. Golden pass samples also omit required intake formats.
  - Continuity blockers: `curriculum.md` still defines OmniSupply Operations while Unit 0.1 and the curriculum map introduce OmniCart; the map returns to OmniSupply at the capstone; and the ledger names a different parallel entity than the worked example.
  - Verification performed: all eight content/concierge structural checks passed; lesson lint reported 0 advisories; Mermaid check passed 3/3; `npm run test` passed; rendered `/units/0.1` accessibility/copy checks passed 12/12. These checks do not cover the semantic and end-to-end failures above.
